<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Приглашение для Маши ❤️</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #ffe5ec 0%, #ffc2d1 100%);
            padding: 20px;
            overflow-x: hidden;
        }

        .card {
            background: white;
            padding: 40px 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(229, 56, 118, 0.15);
            text-align: center;
            max-width: 500px;
            width: 100%;
            position: relative;
            transition: all 0.3s ease;
        }

        .heart-icon {
            font-size: 50px;
            color: #ff477e;
            margin-bottom: 20px;
            animation: heartbeat 1.2s infinite;
        }

        @keyframes heartbeat {
            0% { transform: scale(1); }
            20% { transform: scale(1.1); }
            40% { transform: scale(1); }
            60% { transform: scale(1.15); }
            80%, 100% { transform: scale(1); }
        }

        h1 {
            color: #ff477e;
            font-size: 28px;
            margin-bottom: 20px;
        }

        p {
            color: #4a4a4a;
            font-size: 18px;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        .btn-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            height: 50px;
            position: relative;
        }

        button {
            padding: 12px 35px;
            font-size: 18px;
            font-weight: bold;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: transform 0.1s ease, background-color 0.2s ease;
        }

        #yesBtn {
            background-color: #ff477e;
            color: white;
            box-shadow: 0 5px 15px rgba(255, 71, 126, 0.4);
        }

        #yesBtn:hover {
            background-color: #ff1f60;
            transform: scale(1.05);
        }

        #noBtn {
            background-color: #f0f0f0;
            color: #7a7a7a;
            position: absolute;
            left: calc(50% + 10px);
        }

        /* Класс для финального экрана */
        .success-text {
            display: none;
            color: #2a9d8f;
            font-size: 22px;
            font-weight: bold;
            margin-top: 20px;
            animation: fadeIn 0.5s ease forwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

    <div class="card" id="mainCard">
        <div class="heart-icon">❤️</div>
        <div id="contentArea">
            <h1>Привет, Маш!</h1>
            <p>Долго думал, что бы придумать такое интересное, и вот... Приглашаю тебя на один денёк съездить в Рыбинск! Погулять, повеселиться, провести время вдвоём и стать ещё ближе друг к другу. Я тебя очень люблю! Ну что, погнали?</p>
            
            <div class="btn-container" id="btnBlock">
                <button id="yesBtn" onclick="acceptInvitation()">Да!</button>
                <button id="noBtn" onmouseover="moveButton()" onclick="moveButton()">Нет</button>
            </div>
        </div>
        <div class="success-text" id="successMessage">Ура! 😍 Я знал, что ты согласишься! Собирай вещи, Рыбинск ждет нас! 🚀</div>
    </div>

    <script>
        const noBtn = document.getElementById('noBtn');
        const yesBtn = document.getElementById('yesBtn');
        const btnBlock = document.getElementById('btnBlock');
        const contentArea = document.getElementById('contentArea');
        const successMessage = document.getElementById('successMessage');

        // Функция, которая заставляет кнопку "Нет" убегать
        function moveButton() {
            // Переводим кнопку в абсолютное позиционирование относительно всего экрана, если еще не перевели
            noBtn.style.position = 'fixed';
            
            // Вычисляем случайные координаты в пределах видимости экрана
            const padding = 50;
            const maxX = window.innerWidth - noBtn.offsetWidth - padding;
            const maxY = window.innerHeight - noBtn.offsetHeight - padding;
            
            const randomX = Math.max(padding, Math.floor(Math.random() * maxX));
            const randomY = Math.max(padding, Math.floor(Math.random() * maxY));
            
            noBtn.style.left = randomX + 'px';
            noBtn.style.top = randomY + 'px';
        }

        // Функция при нажатии "Да"
        function acceptInvitation() {
            contentArea.style.display = 'none';
            if(noBtn) noBtn.style.display = 'none';
            successMessage.style.display = 'block';
            
            // Маленький салют из сердечек в консоли или можно добавить эффектов
            createHeartsExplosion();
        }

        // Эффект падающих сердечек при согласии
        function createHeartsExplosion() {
            for (let i = 0; i < 30; i++) {
                const heart = document.createElement('div');
                heart.innerHTML = '❤️';
                heart.style.position = 'fixed';
                heart.style.left = Math.random() * window.innerWidth + 'px';
                heart.style.top = window.innerHeight + 'px';
                heart.style.fontSize = Math.random() * (30 - 15) + 15 + 'px';
                heart.style.transition = 'all ' + (Math.random() * 2 + 1) + 's ease-out';
                heart.style.zIndex = '1000';
                document.body.appendChild(heart);

                setTimeout(() => {
                    heart.style.transform = 'translateY(-' + (window.innerHeight + 100) + 'px) translateX(' + (Math.random() * 200 - 100) + 'px)';
                    heart.style.opacity = '0';
                }, 50);

                setTimeout(() => {
                    heart.remove();
                }, 3000);
            }
        }
    </script>
</body>
</html>
